#!/bin/sh
# AREDN starts redsocks only when wan3 proxy mode is selected. Disable the
# generic package instance so it cannot occupy the private listener first.
/etc/init.d/redsocks disable >/dev/null 2>&1 || true
/etc/init.d/redsocks stop >/dev/null 2>&1 || true
exit 0
